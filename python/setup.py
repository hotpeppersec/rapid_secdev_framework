# -*- coding: utf-8 -*-

from setuptools import setup

setup(
   name='franklin',
   version='1.0',
   description='A useful module',
   author='Franklin',
   author_email='',
   packages=['franklin'],  #same as name
   #install_requires=['foo', 'bar'], #external packages as dependencies
)
